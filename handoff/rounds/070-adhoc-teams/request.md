# 070 后台拖拽编队与临时队伍

## 背景

069 做了散人池。这轮做设计 8.8.2：赛事管理员在后台把散人拖进临时队伍，编队完成即已通过；成员截止前可退出，管理员可移回散人池或解散；三封邮件。

## 本轮范围

做：

- 设计 v1.7.1：8.8.2 去掉「070 轮实现」标记；12.8.2 `Registration.team` 可空、`team_name` 非空约束；12.8.4 新动作 `form_team` / `member_left` / `dissolve`，操作方加 `member`；10.2 三封邮件；13.4 `/registrations/<id>/leave/`；13.13.4 事件；14.2 后台「队伍编排」页；附录 B、D
- 模型与迁移：`Registration.team` null；检查约束 `~Q(team_name="")`；枚举新增
- 服务：`form_teams()`（服务端校验人数、队名、一人一队、逐人队员校验与名单冲突；建队即已通过，日志 `form_team`；调整记 `sync_roster`）、`leave()`（删名单行、回池、日志 `member_left`、最后一人退出自动解散）、`dissolve()`；`withdraw()` 拒绝临时队伍；假设 `team` 非空的地方逐处加分支
- 后台编排页：照内战 032 的拖拽页，普通表单 POST；固定一个「新队伍」区块；解散按钮单独表单
- 前台：报名详情页临时队伍分支（无战队链接、「退出队伍」）；赛事页已报名列表标「临时队伍」不带链接；个人中心状态
- 邮件：`core.mail.emails_for_groups()` 泛化自 moderation；已编入、移出或解散、成员退出（发赛事管理员组）
- 账号注销：已编入的先退出队伍
- 测试与变异；`test_chapter15_audit.py` 的操作方 pin；地址清单

明确不做：按段位自动初分（用户选了手动拖拽）；临时队伍的联系方式导出以外的新后台功能。

## 验收标准

本地检查全绿；新守卫逐个变异被抓到；CI 绿。
