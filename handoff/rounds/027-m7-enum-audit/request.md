# 027 附录 B 枚举值核查

026 核查了附录 C 的**数字**，这轮核查附录 B 的**枚举值**。

这些值不是内部细节：它们出现在 URL、API 响应和 Webhook 请求体里。改一个名字对已对接的上游就是破坏性变更，而且改的时候不一定有人想起来。

## 任务

把附录 B 里每个枚举的取值和代码比一遍，写成测试。

## 验收标准

1. `ruff` / `pytest` / `makemigrations --check` / 生产 `check --deploy` 干净
2. 贴出逐个比对结果
3. git 提交，信息以 `027:` 开头
