# 041 补上 039 剩下的两个测试空白

039 轮的变异测试找到 3 个空白，040 补了预渲染安全闸。剩下两个：

1. **签名比对**（017）：`hmac.compare_digest` 改成 `==`，全部测试照过。设计 11.2.3 第 5 步明写「用常数时间比较校验签名」。
2. **解散战队的检查**（012）：`teams/services.py` 里有 4 处 `if team.is_disbanded:`。

## 任务

### T1 签名

时序泄漏没法在单元测试里可靠地演示，所以断言**原语**：`verify()` 必须调用 `hmac.compare_digest`。另外补一组结果断言（对的签名、大小写、差一位、换密钥、改内容、空签名）。

### T2 解散战队

039 只改了第一处。**这轮四处逐个变异**，确认哪几处真的没测试，再补。

## 验收标准

1. `ruff` / `pytest` / `makemigrations --check` / 生产 `check --deploy` 干净
2. 每处补上的测试都要变异测试确认咬得住
3. git 提交，信息以 `041:` 开头
