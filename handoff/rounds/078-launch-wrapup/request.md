# 078 上线前收尾

## 背景

M8 的功能（067–073）和前台重做（074–077）都做完了，STATUS 里原来的「074 上线前收尾」顺延到这一轮。另外 074–077 里记下了几件小事留到这里。

**本会话没有测试机的 SSH 密钥**（`AGENTS.md`：登录用户和密钥在开发者本机），部署只能写清步骤交给用户。

## 本轮范围

**做**：

1. 保留地址片段补上 `search`：071 加了 `/search/` 路由，但首页下仍能建网址片段为 `search` 的页面，建了会被搜索页盖住
2. 入队申请的位置名「重装」改成「坦克」（模型字段名、`role_labels()`、申请表单），和设计、内战、个人报名一致；字段中文名的迁移
3. 手机上个人中心的横向标签条打开时滚到当前项（`static/js/app.js`，外部脚本，符合 CSP）
4. 设计 13.13.3 占位区域表里账号区域那一行改成现在的样子（登录后是头像、昵称和下拉菜单；「投稿」按钮从来不在账号区域）
5. `handoff/REVIEW-GUIDE.md` 加 074–077：最值得复核的地方
6. `STATUS.md` 的上线计划更新：测试机从 066 升级到 078 的步骤、合并分支

**不做**：部署本身（没有权限）；新功能。

## 验收标准

- 整组检查全绿；新规则各有测试并做变异
- 标签条滚动在真浏览器里验证
