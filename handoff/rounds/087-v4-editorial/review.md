# 087 复核结果（自查）

## 结论

**通过（自查）**，待独立复核。

## 验证记录

1. **变异的基线**：`63 passed` 之后才开始改坏；9/9
2. **摘要**：设计 5.2 的文章字段写着「摘要，列表页显示」，第一版图片卡把摘要去掉了，跑测试时被旧测试拦下；改成资讯列表上显示两行、首页不显示，设计 13.2.6 同步
3. **预渲染**：资讯列表、文章页、普通页面都是静态页；改动全在模板和样式里，没有新的动态部分
4. **深色模式**：文章页深色截图正常（标签、链接、分隔线都跟着变）

## 判断里最没把握的

1. 文章页把「同栏目最新」从右栏挪到正文后面，桌面上正文两边是空白；这是用户指出「没居中」之后定的
2. 列表卡片没有作者了（v3 有首字头像和作者名）

## 文档更新

- `docs/design.md`：13.2.6、13.2.7
- `handoff/STATUS.md`
