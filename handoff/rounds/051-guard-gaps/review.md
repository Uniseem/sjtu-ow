# 051 复核结果

## 结论

**通过。**

## 验证记录

| 验证 | 结果 |
|---|---|
| 每条测试对准自己那道闸 | 15 处逐个拆掉，每处被抓到的都是为它写的那条测试，没有互相替代 |
| 源码没被改动 | 变异在导出的副本里跑；`git status` 只有测试文件 |
| 全量 | 675 passed |

## 这轮最值得记的

**042 的「等价」判断本身也需要验证。** 042 说上传时的大小检查是等价变异，因为后面还有一道——那句话里的「后面还有一道」是假设，不是事实。这和 039 说「找不到队长上限的代码」、043 说「026 附录 C 全部对得上」是同一类：**用一句没核实过的话支撑一个结论**。

## 判断里最没把握的

- **Google 400 和空字体文件那两条断言的是提示文字**，不是行为。拆掉检查后照样会报错，只是提示变了。测提示文字是对的（用户看到的就是它），但以后改文案时这两条测试会跟着红，这是预期的
- **首页置顶那条**：`home.pinned_articles = [...]` 是在内存里设子项，模拟后台一次性提交。没有真的走一遍后台表单
