# 051 补 042 找到的测试空白

## 背景

用户 2026-09-18 定了：测试机上的部署**算测试环境**；SSH 密码登录**不管**。随后按上线计划开工，这是第一步。

042 轮把守卫语句逐个拆掉跑测试，跑了 40 个，25 个没被发现，其中 14 个判为真空白（清单和理由在 `rounds/042-guard-sweep/report.md`）。另外 042 把 `core/fonts/forms.py:78`、`:139` 两处上传大小检查判为「等价」，依据是 `core/fonts/processing.py:60` 还有一道长度检查——**但那一道当时还没跑到**，判断悬着。

## 任务

1. 14 处各补一条测试，每条的构造要**只有那道闸能拦住**，否则拆掉它时别的检查会顶上，测试照样绿：
   - 字段加密 3 处：空钥匙、换钥匙后读、换钥匙后存
   - 功能名拼错
   - B 站嵌入 2 处：短链跳出 B 站、没有 BV 号
   - 字体 7 处：声明长度超限、实际长度超限、下载到空文件、Google 返回 400、样式表里没有 woff2、添加字重时既没文件也没地址、上传空文件
   - 首页置顶：页面 `clean()` 在子项还没保存时就拦住第 4 篇
2. 先单独变异 `processing.py:60`，看它有没有测试；没有就补，再据此确认 042 对 `forms.py:78`、`:139` 的判断
3. 用 042 的脚本对这些位置逐个变异，确认每条新测试都会红
4. `STATUS.md` 记下用户的两个决定

## 验收标准

1. 本地四项检查干净，推送后 CI 绿
2. 变异结果全部是「被抓到」，输出原样放进报告
3. 源码全部还原，`git diff` 只有测试和文档
