# 005 复核结果

## 结论

**通过。M1 完成。** 004 留下的 SMTP 加密问题已经真正解决，我用自己的用例验证过；个人资料、联系方式、功能权限、`init_site` 都符合设计。

## 验证记录

| 项目 | 我的验证方式 | 结果 |
|---|---|---|
| ruff / format | 本地重跑 | 通过，116 个文件 |
| pytest | 本地重跑 | **97 个测试通过** |
| `makemigrations --check` / `check --deploy` | 本地重跑 | 无遗漏迁移；0 issues |
| **T1 加密（004 的遗留问题）** | 用我在 004 里让它失败的同一组用例 | ① ORM 直接写入 → 数据库里是 `gAAAAA…` 密文 ✅ ② 原样再保存一次 → 没有重复加密，仍能解出原值 ✅ ③ 手工塞进明文再读 → 正常返回明文，保存后自动变密文 ✅ |
| T2 段位编码 | 自己按附录 A 核对 | 钻石 3 = 22、前 500 = 40、未定级为空；0–40 全部往返一致 ✅ |
| T2 BattleTag | 造重复和非法数据 | 大小写不敏感唯一生效；`nohash`、`a#123`、超长名称、位数不足、含空格全部被拒 ✅ |
| T3 联系方式 | 造重复和非法数据 | `(user, type)` 唯一生效；QQ 填字母、手机号填 3 位、微信过短都被拒 ✅ |
| T5 `can_use` | 自己构造三层组合 | 默认 True → 给「校外用户」组加限制后 False → 给该用户单独放行后 True → 用户停用后 False ✅ |
| T6 `init_site` | 连跑两次并比对组和权限快照 | 幂等；七个预置组齐全；赛事管理员有 `access_admin` + `view_contactmethod`，投稿者只有 `access_admin` ✅ |
| T4 个人中心 | 测试客户端 + 浏览器 | 四个页面都 200，模板无内联 style，未登录访问跳登录 ✅ |
| T4 浏览器实测 | 真的登录进去看 | `/me/game-accounts/` 正确显示「钻石 3 / 黄金 4 / 前 500」和「段位更新于 0 分钟前」；点「编辑」后 HTMX 换成段位下拉表单；**全程 0 条控制台报错** ✅ |
| Docker / git | `docker images` / `git status` | 镜像 9 分钟前重建、ID 与报告一致；工作区干净 ✅ |

## 必须修

无。

## 建议修（可以并进下一轮）

1. **Wagtail 自带的 `Editors` 和 `Moderators` 两个组还在**，和我们自己的「内容编辑」并存，管理员在后台会看到三个名字相近的组，容易分错人。建议 `init_site` 把这两个组删掉或改名
2. 我 004 复核时留下的测试用户之前还在库里，本轮我已经清掉。以后如果需要演示数据，建议加一个 `seed_demo` 命令，和真实数据分开

## 认可的判断

- 不走 `TextField.get_prep_value()`、避免「每次写入都换新 token」这个坑，判断正确
- `/me/teams/`、`/me/registrations/` 本轮不做页面但在导航里标成即将开放，比直接 404 好
- `max_game_accounts` 的帮助文本去掉「后续里程碑使用」是对的，本轮确实用上了
- 手机端 6 个标签需要横向滑这个观察属实，等视觉风格定了一起调，不用现在改

## 文档更新

无需改设计文档。功能权限菜单挂在「设置」下与 14.1 节的「用户 → 功能权限」略有出入，但都在超级管理员可见范围内，属于后台布局细节，不值得为它改文档。
