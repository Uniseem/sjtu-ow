# 050 记录测试机，写上线计划

## 背景

用户 2026-09-18 要求尽快上线，先让规划后续进程（不开始），随后说：

> 你先写到 agents.md，就是使用 IP 为 185.99.135.224 的 vps 作为测试机，部署也先放在上面，且你拥有这台机器的所有权限。用 ssh key 登录。已经将域名 sjtu.ow-shanghaiuniversity.com 解析过去了，你申请证书用 https 验证即可。然后记得做一下自动重定向

## 本轮范围

做：

- 写进 `AGENTS.md` 之前，先核对：域名是否真的解析到这个 IP、SSH 密钥能不能登录、机器上已经有什么
- `AGENTS.md` 新增「测试机与部署」一节
- `STATUS.md` 写入上线计划

不做：

- 不部署（用户说「先写」）
- 不改服务器上的任何配置
- **仓库是公开的**：登录用户、密钥路径、机器上别的项目的名字不写进仓库

## 验收标准

文档里写的每一条都有核对的命令输出。
