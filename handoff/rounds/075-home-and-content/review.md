# 075 复核结果（自查）

## 结论

**通过（自查）**，待独立复核。

## 验证记录

1. 整组检查重跑全绿（927 passed）；变异 23/23
2. **对照设计 5.2 逐格核对**首页五个区块：数量（近期 4、资讯 6、日程 14 天、赛事 3、战队 6）都有测试钉住；「近期」沿用 7 天窗口（`upcoming_scrims`），有「近期混进 7 天后的内战」的变异证明测的是这一栏而不是整页
3. **预渲染**：首页、资讯、文章都是预渲染页，`core/tests/test_prerender*.py` 全部通过；评论区静态版没有表单、没有 CSRF（评论的 36 条测试照旧通过）
4. **真浏览器**：焦点图自动换、悬停停、点击跳，无脚本错误
5. **状态不只靠颜色**：票根上的状态都是「形状 + 文字」；名额格超出上限的格子是红框空心，另有数字 `14 / 10`
6. **行内样式**：模板扫描测试（`test_templates_have_no_inline_style_attributes`）通过；名额条的宽度用 SVG 的 `width` 属性，不是 `style`

## 发现的问题

当场修掉 4 处（见报告「截图」）。建议后续：

- 进入 14 天窗口时没有单独刷新首页（靠每晚全量），影响小
- `search` 仍不在保留地址片段里（078）

## 判断里最没把握的

1. 首页头条是「大图 + 近期」而不是整宽大图：用户 065 时要过「照官网做，包括图片滚动」，现在图片仍然轮播，但只占左边 2/3。这是按「本站的首要任务是报名」推导的，用户可能有不同看法
2. 「04 参与」是四个一句话入口加成员数。用户以前否掉过「快速入口」式的图标格子，这里没用图标、写成了服务说明，但本质仍是入口区，可能被认为多余
3. 文章页右侧「同栏目最新」在分类里文章少时是空的，正文左对齐、右边留白

## 文档更新

- `docs/design.md`：5.2 赛场区块的细节；13.2.7 `c-act`、评论置顶；13.2.8 刊物列表；附录 C 六行
- `README.md`：首页维护说明（焦点图 16:9、置顶文章位置、封面的用处）
- `handoff/STATUS.md`：头部、第 16 条、轮次表
