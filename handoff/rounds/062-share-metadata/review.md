# 062 复核结果

## 结论

**通过。**

## 判断里最没把握的

- **QQ 的真实预览没看过**：测的是页面 HTML 里的标签。QQ 抓取预览时会不会因为服务器在境外、证书链、抓取频率之类的原因拿不到，要在测试机上真的贴一次链接才知道
- **内战标题里的时间**用的是服务器配置的时区（北京时间），格式「5月3日 19:30」是我定的，设计只写了「活动名称 + 开始时间」
- **战队简介里有换行**时，`og:description` 会原样带上换行。大多数平台会自己处理，没做清理
