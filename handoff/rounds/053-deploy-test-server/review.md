# 053 复核结果

## 结论

**部分完成。** 测试环境能用，四个问题里一个已改、三个记录待修。

## 这轮最值得记的

**第一次在真机上部署，四个问题里三个本地永远发现不了**：

- `--env-file` 的问题：本地开发不用 Compose
- 健康检查 400：CI 只跑 `docker build`，从没把容器跑起来过
- 重启策略：要等服务器重启才会暴露

这是 044「本地全绿不等于 CI 全绿」的下一层：**CI 全绿也不等于能部署**。部署清单只有在真机上走过一遍才算数。

健康检查那条是**部署前预判到的**（读了生产配置里的 `ALLOWED_HOSTS` 和 `SECURE_SSL_REDIRECT`），部署后实测确认。

## 判断里最没把握的

- **证书 90 天后能不能自动续期**：Caddy 会自动续，证书存在 `caddy_data` 卷里。没有等到续期那天验证
- **国内访问速度**：只从开发机（经过代理）访问过，没从校园网、三大运营商实测
- **README 里「一份代码目录只能跑一套环境」**是从 Compose 配置推出来的，没有实际在同一台机器上跑两套试过
