# 085 复核结果（自查）

## 结论

**通过（自查）**，待独立复核。M9 到这一轮做完。

## 验证记录

1. **变异的基线**：脚本先跑一遍，`57 passed` 之后才开始改坏；12/12
2. **菜单图标**：测试除了数图标，还断言 7 项的图标名互不相同（变异「两项共用一个图标」被抓到）
3. **卡片样式**：原来的测试只看模板里有没有 `c-auth` 类名，删掉样式里的圆角不会红；本轮补了对 `input.css` 里 `.c-auth` 块的断言，变异「卡片样式没了圆角」被抓到
4. **评论数**：原来没有测试管评论区标题的数量样式；本轮在评论静态页测试里加了一条
5. **手机**：登录、注册、个人中心 375px 宽都不横向溢出。个人中心在手机上是横向标签条，当前项带对勾
6. **粗线扫描**：`test_no_front_template_draws_v2_heavy_rules` 扫全部前台模板，往首页模板里加一条 `border-b-2` 会红

## 发现的问题

- 已当场修：`c-auth` 卡片和评论数两处只有类名、没有管样式或根本没测（见上面 3、4）

## 判断里最没把握的

1. 手机上侧栏换成标签条后不带图标，和桌面不一致；图标放进窄标签会挤，是主观取舍
2. 「注册以后可以」在手机上不显示（沿用 077 的做法），手机用户看不到注册的用处
3. 粗线扫描只认四种写法（`border-fg`、`border-t-2`、`border-b-2`、`border-y-2`），别的 v2.0 痕迹靠逐页测试

## 文档更新

- `docs/design.md`：v3.0.1，13.5、13.2.7、附录 D
- `handoff/REVIEW-GUIDE.md`：v3.0 一节补 082–085
- `handoff/STATUS.md`：M9 完成，测试机升级步骤改到 085
