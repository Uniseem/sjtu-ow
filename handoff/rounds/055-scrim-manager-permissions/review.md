# 055 复核结果

## 结论

**通过。**

## 这轮最值得记的

**一句「等到以后」的提示藏了一个真缺陷，而且藏了 50 轮。** 005 轮写下「内战管理员的内战管理权限（M6）」，M6 在 020、021 轮做完，这句提示一直留着。每次跑 `init_site` 都会打印它，但它看起来像待办清单，不像错误。

是 053 第一次在真机上部署、逐行看输出时才注意到。

**同类的东西值得搜一遍**：代码里还有没有别的「等到某个里程碑」「以后再做」的占位？这轮没搜，列进 `STATUS.md`。

## 为什么测试一直没发现

内战的测试全部用超级管理员登录。**超级管理员绕过所有权限检查**，所以用它测后台，等于没测权限。赛事那边有 `manager` 夹具（赛事管理员组成员），内战没有。

## 判断里最没把握的

- **`delete_scrim` 给不给**：照赛事的做法给了。设计 4.1 只说「创建内战、勾选上场、调整分队」，没说能不能删。后台对已发布的内战能不能删，走的是后台自己的逻辑，这轮没动
- **没有在测试机上用内战管理员账号真的登录试**：测试机上还没有任何账号（超级管理员要用户自己建）。本地测试覆盖了后台页面能打开
