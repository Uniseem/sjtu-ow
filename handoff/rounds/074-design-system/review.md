# 074 复核结果（自查）

## 结论

**通过（自查）**，连做轮次，待独立复核。设计体系、外框、组件、样张页、错误页都做完并验证过；页面内部的改版按计划在 075–077。

## 验证记录

1. **重跑整组检查**：ruff、format、`tailwind build --force`、全量 pytest（919 passed）、迁移检查、生产 `check --deploy`、错误页重新生成，全部通过（输出见报告）
2. **变异**：19 处，第一轮 18/19，幸存的是等价变异（页脚两处写了「非官方」），改成两处都删后被抓到，19/19
3. **对照设计原文逐条核对** 13.2.7 组件表：每个组件在 `input.css` 里都有对应的类，并且都出现在样张页上（`c-masthead`、`c-drawer`、`c-footer`、`c-crumbs`、`c-pagehead`、`c-sectionhead`、`c-btn` 四种、`c-tag`、`c-status` 七种、`c-rank`、`c-roles`、`c-slots`、`c-stat`、`c-facts`、`c-table`、`c-rowlist`、`c-ticket`、`c-daystrip`、`c-tiles`、`c-person`、`c-avatar`、`c-field`、`c-steps`→实现名 `c-step`、`c-notice`、`c-toast`、`c-empty`、`c-pager`、`c-tabs`、`c-prose`）。`c-feature`（焦点图）和 `c-comments` 只写了样式没上样张页：它们要真实的轮播脚本和评论数据，075 接上时再加
4. **对比度逐对算过**（WCAG 公式）：浅底次要文字 6.8、浅底红字 9.2、深底次要文字 7.8、深底红字 5.1、红底白字 7.0、三种状态色 5.3–5.5，都过 AA；`ink-3` 3.75 只够大字和非关键文字，设计里写了这个限制。设计里原来的三个对比度数字是用早一版底色算的，按实测改了
5. **截图**：1280 和 390 两种宽度看了样张页全部 14 个区块、页头页脚、404；截图环境把开源字体注册成苹方和 DIN，尽量接近访客设备
6. **预渲染安全**：`core/tests/test_prerender*.py` 全部通过——新的账号下拉（含「退出」）只在已登录的实时渲染里出现，预渲染页里是「登录」「注册」

## 发现的问题

**当场修掉**：表格在网格里被拉高（`align-self: start`）；一个画乱了的图标删掉；样张页的权限判断从 `is_staff` 改成后台权限（否则内容编辑这类非超管的后台账号看不到）；抽屉从复选框改成 `<details>`（键盘可达）。

**建议修（留给后续轮次）**：

- `c-feature`、`c-comments` 上样张页（075）
- 设计 13.13.3 表里账号区域那一行的旧说法（078）
- 保留地址片段缺 `search`（078）

## 判断里最没把握的

1. **风格本身**。用户否过三次「AI 味」，这次是从头推导的，但用户没看过。最可能被挑的是：深色页头加红色切角方块这个标志是否太朴素；编号 `01`、`02` 和英文眉标会不会被看成「装饰」
2. **参考网站只看到了一部分**：官网被网络策略拦了，守望先锋、明日方舟的数值来自 GitHub 上的副本；社区、组织类网站的调研子任务没有在提交前返回，13.2.1 表里那几行是凭已有了解写的
3. **过渡期页面是乱的**：这一轮提交后、077 之前，内容区大多没有样式。只要不部署就没有影响，但如果有人在中间部署，会看到半成品

## 文档更新

- `docs/design.md` v2.0（见报告）
- `README.md`：技术栈一行；「视觉风格与首页」改写（令牌、过滤器、样张页、过渡期）
- `AGENTS.md`：坑里加了三条（Tailwind 色板关掉了、Linux 容器里截图要注册字体、网格里的表格会被拉高）
- `handoff/STATUS.md`：头部、第 16 条决定、上线计划改成 075–078、里程碑、轮次表
