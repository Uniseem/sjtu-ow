# 036 把所有「未验证」翻出来重新问一遍

034 和 035 连着两轮，都是把一条我自己标成「要等真机」的事拆开之后当场做掉的，而且都找到了东西（034 找到 N+1，035 把设计断言变成可观测证据）。

**同一个毛病犯两次就不是偶然。** 这轮系统地做一次：把 008–035 所有报告里写过的「未验证 / 要真机 / 做不了」全部翻出来，逐条重新问一遍「真的整块都做不了吗」。

## 验收标准

1. 列出全部条目和重新判断的结果
2. 能做的当场做掉
3. `ruff` / `pytest` / `makemigrations --check` / 生产 `check --deploy` 干净
4. git 提交，信息以 `036:` 开头
