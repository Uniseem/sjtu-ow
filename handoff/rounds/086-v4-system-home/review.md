# 086 复核结果（自查）

## 结论

**通过（自查）**，待独立复核。

## 验证记录

1. **变异的基线**：脚本先跑一遍，`96 passed` 之后才开始改坏；14/14
2. **深色模式**：对比度测试按模式参数化，深色值由 `@theme` 的浅色值加深色块覆盖得出；深色块漏写任何一个有色值的令牌（`white`、`black`、`night`、`night-2` 除外）会红
3. **别名**：v3 名字全部是 `var()`，没有自己的色值，所以深色模式下也跟着变；测试抽查了四个
4. **预渲染**：首页是静态页，首屏按钮对所有人一样；设计 5.2 原先写的「已登录时换成报名内战」和预渲染冲突，已改掉
5. **官方图片**：只在本地 `media/`（gitignore），`git status` 里没有图片文件

## 发现的问题

- 已当场修：手机上数字条「3 年 125 天」折行
- 已当场修：设计文档把图片卡叫 `c-card`，和 v3 里还在用的「整个框可点的面板」重名；图片卡改叫 `c-media`
- 已当场修：错误页的 `body` 还是两团渐变色

## 判断里最没把握的

1. 首屏只有标题、一句话和两个按钮，没有别的信息；用户之前要过首屏里放数字，这次放在紧挨着的数字条里
2. 近期只放一场赛事（截止最早的）。同时有两场以上在报名时，第二场只能在赛事页看到
3. 没重做的页面现在是新颜色 + 旧版式，看起来会有点不统一，到 089 才完全一致

## 文档更新

- `docs/design.md`：v4.0（13.2、13.3、5.2、12.4.1、13.13.4、19.2、附录 D）
- `README.md`：视觉风格与首页
- `AGENTS.md`：截图方法、深浅两套颜色的坑
- `handoff/REVIEW-GUIDE.md`：v4.0 一节
- `handoff/STATUS.md`
