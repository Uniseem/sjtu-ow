# 089 M10 收尾：个人中心、表单、评论、错误页，去掉 v3 残留

## 背景

M10 最后一轮。086–088 重做了首页、刊物页、赛场与名册；个人中心、登录注册、报名和战队表单、评论区、错误页还靠 086 的 v3 令牌别名换色，样式表里还有 v3 的令牌名、色调变量、玻璃变量和小于 14px 的字。

## 本轮范围

**做**：

1. 样式表里的 v3 令牌名、`--tone-*`、`--glass-*` 全部换成 v4 名字，`@theme` 里的别名和色调变量删掉；模板里的 `text-fg-red`、`border-rule`、`bg-surface-high` 换成 v4 工具类
2. 圆角只留 4px、8px、圆形三级；模板里的 `rounded-md`、`rounded-lg` 换成 `rounded-sm`
3. 全站字号不小于 14px（样式表和模板都查）
4. 评论区标题的数量从徽标改成「N 条评论」；报名表单的分步去掉 `01`、`02` 编号
5. 错误页和维护页跟随系统深浅色，去掉 FORBIDDEN / MAINTENANCE 这类英文标签
6. 删掉没有模板在用的旧样式
7. 设计 13.2.6（`c-step`）、13.15；REVIEW-GUIDE、STATUS（M10 完成、测试机升级步骤改到 089）；AGENTS.md 记 `default` 过滤器的坑

**不做**：新功能；测试机部署（SSH 密钥在用户那边）。

## 验证

- 样式表和前台模板里找不到任何 v3 名字
- 没有小于 14px 的字
- 错误页浅色、深色两套值都和站点一致，没有英文标签
- 变异（先跑基线）；截图深色、375
